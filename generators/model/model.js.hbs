import mongoose from 'mongoose'

const {{ properCase name }} = new mongoose.Schema({
    type: {
        type: String,
        default: 'User'
    },
    name: {
        type: String
    },
    username: {
        type: String,
        required: true,
        unique: true
    },
    password: {
        type: String,
        required: true
    }
})

export default mongoose.model('{{ camelCase name }}', {{ properCase name }})
