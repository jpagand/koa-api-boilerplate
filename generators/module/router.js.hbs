/*
 *
 * {{ lowerCase name }} routes
 *
 */
{{#if wantPrivate}}
import {ensureUser} from 'middleware/validators'  // eslint-disable-line
{{/if}}
{{#if wantValidators}}
import * as validators from './validators' // eslint-disable-line
{{/if}}

import * as {{ camelCase module }} from './controller'

export const baseUrl = '/{{ lowerCase module }}'

const routes = [
    /* GENERATED: ROUTES DO NOT TOUCH */
]

export default routes